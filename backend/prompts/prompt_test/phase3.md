````
# Phase 3 — Code Generation (Universal)

ROLE
You are a Senior Test Lead + Code Generator. Read a local Markdown test matrix and generate runnable Jest tests for the **specified test suite** of the “Order in Livestream” feature. Produce real, runnable code (no prose), with mocks, helpers, and adapters as required.

INPUTS (PARAMETERIZE THESE)
1) Test matrix file (Windows path):
    D:\Workspace\Kicks-Shoes\backend\prompts\output_phase2.md

2) Target project structure (write files exactly here):
   backend/
   ├── src/
   │   └── <OPTIONAL_SOURCE_FILES>.js
   ├── tests/
   │   ├── <SUITE_SLUG>.test.js            <-- WRITE TESTS HERE (file name must come from the suite)
   │   ├── _helpers/
   │   │   └── testUtils.js                <-- WRITE/UPDATE shared helpers here (idempotent)
   │   └── mocks/
   │       └── productService.mock.js      <-- WRITE/UPDATE MOCKS HERE
   ├── coverage/
   │   └── index.html                      <-- generated by `npm test -- --coverage`
   ├── prompts/
   │   └── log.md                          <-- append a short generation log here
   └── README.md                           <-- append “how to run tests” if not present

3) Suite selection (case-insensitive, exact heading text):
   <SUITE_HEADING>
   e.g. "Test Suite 3: Chat Message Handling"

4) Target Under-Test (per suite) — choose the correct primary subject for this suite:
   - Suite 1 → orderDetection.service.analyzeMessage(messageData, streamData, userData)
   - Suite 2 → orderDetection.service.extractProductInfo(rawMessage, streamData)
   - Suite 3 → liveStreamService.handleChatMessage(socketId, messageData) + Socket.IO integration
   - Suite 4 → potentialOrderController.* (CRUD/filter/stats)
   - Suite 5 → updateOrderStatus(req,res) + OrderService.createOrder(orderData)
   - Suite 6 → Mixed edge & error cases across services (focus on boundaries/exceptions)
   - Suite 7 → Integration/E2E (Supertest + Socket.IO client; can spin up express app instance/mock)

FILE NAMING RULES
- Derive <SUITE_SLUG> by lowercasing the suite title and replacing spaces/“&” with hyphens:
  Examples:
  - “Message Analysis & Order Detection” → message-analysis-order-detection
  - “Chat Message Handling” → chat-message-handling
  - “Order Auto-Creation” → order-auto-creation
- Final test filename: tests/<SUITE_SLUG>.test.js

WHAT TO READ & PARSE
- Open the Markdown file at <ABSOLUTE_MARKDOWN_PATH>.
- Locate the section titled exactly (or case-insensitively) <SUITE_HEADING>.
- Extract **ALL** test cases for that suite (IDs, names, categories, preconditions, steps, expected results, priority).
- Count the cases and generate the **exact** same number of Jest `test()` blocks.
- Preserve **Test IDs** literally in test names: `TC-XYZ | <concise scenario>`.

WHAT TO GENERATE (CODE ONLY)

A) tests/<SUITE_SLUG>.test.js
- Create a Jest test suite:
  `describe('Order in Livestream — <SUITE_HEADING>', () => { ... })`
- Generate one `test()` per extracted case with strict `TC-XYZ` prefixes.
- Use **Given–When–Then** comments within each test body.
- Implement realistic assertions from the “Expected Result”.
- Include timing assertions for performance cases.
- Handle security cases via sanitization or non-crash assertions.

B) tests/mocks/productService.mock.js
- Export a basic mockable module:
  ```js
  const productDb = new Map([['HJ6777', { id: 'prod123', sku: 'HJ6777', price: 3500000 }]]);
  module.exports = {
    __set(key, val) { productDb.set(key, val); },
    __clear() { productDb.clear(); },
    findOneBySku(sku) { return Promise.resolve(productDb.get(sku) || null); },
    findOneByInventorySku(invSku) { return Promise.resolve(null); },
  };
````

C) tests/\_helpers/testUtils.js

- Provide VN test data helpers:
  - `makeMessage(content, overrides)`
  - `makeStream(overrides)`
  - `makeUser(overrides)`
  - `normalizePhone(vnPhoneStr)`
  - `perf(fn)`

D) prompts/log.md

- Append:
  `YYYY-MM-DD HH:mm | Generated tests for "<SUITE_HEADING>" (N cases)`

E) README.md

- Add instructions to run tests with coverage.

CODING CONSTRAINTS

- Jest only. No external test libs.
- Prefer CommonJS (`require`).
- No real network/DB; all mocked.
- ≥80% coverage.
- Deterministic tests only.

OUTPUT FORMAT
Return **only code blocks** for:

1. `// FILE: tests/<SUITE_SLUG>.test.js`
2. `// FILE: tests/mocks/productService.mock.js`
3. `// FILE: tests/_helpers/testUtils.js`
4. `// FILE: prompts/log.md`
5. `// FILE: README.md`

BEGIN NOW:

1. Read the markdown file.
2. Parse section <SUITE_HEADING>.
3. Extract cases and count.
4. Generate code files.
5. Output files in the order above.

```

```
